/*
  Based on conditional logic in parent, if it is determined if it is a video, use this component.
*/

import React from 'react';

export const VideoSlider = (props) => {
  return(
    <div>
      video slider
    </div>
  );
}
