/*
  Based on conditional logic in parent, if it is determined if it is a pic, use this component.
*/

import React from 'react';

export const ImageSlider = (props) => {
  return(
    <div className="image-slider">
      Image Slider
    </div>
  );
}
